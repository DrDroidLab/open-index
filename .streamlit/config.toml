[server]
# Local-first tool: allow the UI to be served behind a reverse proxy / preview tunnel.
address = "0.0.0.0"
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false
