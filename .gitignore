# Generated brain storage
*.db
*.db-journal
*.db-wal
.open_index_state.json
.open_index_analytics.db

# Docker env — holds OPEN_INDEX_TOKEN. Commit .env.example instead.
.env

# Default bind mount target for docker compose (BRAIN_DIR).
/brain/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/

# Coverage
.coverage
coverage.xml
htmlcov/

# Generated by deploy/fleet/fleet.py — indexes.yml is the source of truth.
deploy/fleet/docker-compose.generated.yml
deploy/fleet/caddy/
deploy/fleet/nginx/
# Per-deployment config: hostnames, which indexes this host serves, whether auth
# is on. Copy indexes.example.yml to start.
deploy/fleet/indexes.yml
