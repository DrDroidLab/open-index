# GENERATED BY fleet.py — do not edit. Change indexes.yml and re-run `fleet.py up`.

20.83.97.30.nip.io {
	# Load-balancer probe: unauthenticated, and free of any index detail.
	handle /healthz {
		respond "ok" 200
	}

	# aviation-mro-index — explorer UI. No prefix stripping: Streamlit runs with
	# baseUrlPath=aviation-mro-index/ui and expects the full path.
	handle /aviation-mro-index/ui* {
		reverse_proxy ui-aviation-mro-index:8501
	}
	# aviation-mro-index — MCP endpoint. handle_path strips the /aviation-mro-index prefix so the
	# brain receives /mcp.
	handle_path /aviation-mro-index/* {
		reverse_proxy brain-aviation-mro-index:8080 {
			# Streamable HTTP holds responses open; never buffer them.
			flush_interval -1
		}
	}
	# commercial-lines-index — explorer UI. No prefix stripping: Streamlit runs with
	# baseUrlPath=commercial-lines-index/ui and expects the full path.
	handle /commercial-lines-index/ui* {
		reverse_proxy ui-commercial-lines-index:8501
	}
	# commercial-lines-index — MCP endpoint. handle_path strips the /commercial-lines-index prefix so the
	# brain receives /mcp.
	handle_path /commercial-lines-index/* {
		reverse_proxy brain-commercial-lines-index:8080 {
			# Streamable HTTP holds responses open; never buffer them.
			flush_interval -1
		}
	}

	# Directory of what this host serves. Names and URLs only.
	handle {
		header Content-Type application/json
		respond `{
  "aviation-mro-index": {
    "mcp": "https://20.83.97.30.nip.io/aviation-mro-index/mcp",
    "description": "Line and base maintenance for a narrowbody fleet \u2014 ATA taxonomy, fault messages, task cards, MEL entries and defect history. SYNTHETIC DEMO DATA.",
    "read_only": false,
    "ui": "https://20.83.97.30.nip.io/aviation-mro-index/ui"
  },
  "commercial-lines-index": {
    "mcp": "https://20.83.97.30.nip.io/commercial-lines-index/mcp",
    "description": "Commercial-lines placement for an SMB brokerage \u2014 carriers, appetite rules, class codes, state rules and submission playbooks. SYNTHETIC DEMO DATA.",
    "read_only": false,
    "ui": "https://20.83.97.30.nip.io/commercial-lines-index/ui"
  }
}` 200
	}
}
